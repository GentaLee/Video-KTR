# Imported H200 handoff

> Generated snapshot; edit the source branch, not this file.

- SOURCE_COMMIT: `917416d37d8fc62f5af59f2208806bf149784947`
- SOURCE_COMMIT_TIME: `2026-09-23T12:27:58+08:00`
- SOURCE_PATH: `handoff/STATUS.md`
- SOURCE_SHA256: `b2778c844472599920725a3a56ee2ea789a6a91d117b1c77c053b2482f6abd1c`

---

# H200 状态（本分支唯一事实源）

PROFILE: h200
STATUS: RUNNING
LAST_VERIFIED_UTC: 2026-09-23T04:26:07Z

## 工作区责任

本工作区只维护集群 2 / H200；开发分支为 `<owner>/video-ktr-h200`。本次拆分只新增协作文件并从同一 commit 创建分支，保留全部既有未提交改动，不更改训练源码或进程。开发 HEAD 不是正在运行实验的源码身份。

## 当前运行证据

- run：`paired-step0-ktr-20260922T103931Z`，相对产物根 `artifacts/grpo-full/`。
- 启动 provenance：`10bff9c` + dirty worktree；既有 provenance 保存了 diff SHA 与源码哈希，但不能仅凭 diff SHA 重建全部历史内容。不得将当前开发 HEAD 冒充其运行版本。
- 配置：4×H200；Video-R1 的 116,248 条核验视频；4096/512/G4；SDPA；8 帧；5 次时序置换（含 reverse）；每 500 步 checkpoint。
- 最后检查：308/29,062 步，训练日志及 GPU 运算持续推进；这不是实时进度。
- 该 run 的 baseline 尚未启动。与 B200 配置不同，不能跨集群直接作 KTR/baseline 对照。

## 边界与下一步

训练目录与集群 1 CPU 共享，运行期间不覆盖源码、不切换到不同 commit、不从 B200 整支合并。历史未提交改动单独审阅后再归档，不能用 `git add .` 一并上传。当前分支的 describe/development/handoff 工作区增量仍待 H200 负责者审阅，不由 B200 同步器覆盖。

结束后整理本 run 的资源摘要、checkpoint 身份、评测与 token 示例到 `reports/<run-id>/`，脱敏后提交本分支；更新本文并 push，由 B200 负责者导入快照。

RECOVERY: 不重复启动；失败时先核对本 run 日志及保活状态，未经明确任务不操作训练/保活。
